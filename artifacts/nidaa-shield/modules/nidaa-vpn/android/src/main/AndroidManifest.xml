<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SYSTEM_EXEMPTED" />
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
  <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

  <!--
    QUERY_ALL_PACKAGES is required so the "Excluded Apps" screen can list
    the user's installed apps (PackageManager.getInstalledApplications on
    targetSdk >= 30 returns an empty list without it). VPN apps are an
    explicitly approved use-case under Google Play's package visibility
    policy, but a declaration form must still be filed in the Play Console
    when the app is uploaded:
    https://support.google.com/googleplay/android-developer/answer/10158779
  -->
  <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

  <application>
    <service
      android:name="expo.modules.nidaavpn.NidaaVpnService"
      android:exported="false"
      android:permission="android.permission.BIND_VPN_SERVICE"
      android:foregroundServiceType="systemExempted">
      <intent-filter>
        <action android:name="android.net.VpnService" />
      </intent-filter>
    </service>

    <!-- Master toggle tile -->
    <service
      android:name="expo.modules.nidaavpn.NidaaTileService"
      android:label="نداء شايلد"
      android:icon="@android:drawable/ic_lock_lock"
      android:exported="true"
      android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
      <intent-filter>
        <action android:name="android.service.quicksettings.action.QS_TILE" />
      </intent-filter>
    </service>

    <!-- Per-mode quick settings tiles -->
    <service
      android:name="expo.modules.nidaavpn.SmartModeTileService"
      android:label="الدرع الذكي"
      android:icon="@android:drawable/ic_menu_view"
      android:exported="true"
      android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
      <intent-filter>
        <action android:name="android.service.quicksettings.action.QS_TILE" />
      </intent-filter>
    </service>

    <service
      android:name="expo.modules.nidaavpn.GamingModeTileService"
      android:label="توربو الألعاب"
      android:icon="@android:drawable/ic_media_play"
      android:exported="true"
      android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
      <intent-filter>
        <action android:name="android.service.quicksettings.action.QS_TILE" />
      </intent-filter>
    </service>

    <service
      android:name="expo.modules.nidaavpn.FamilyModeTileService"
      android:label="حارس العائلة"
      android:icon="@android:drawable/ic_menu_myplaces"
      android:exported="true"
      android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
      <intent-filter>
        <action android:name="android.service.quicksettings.action.QS_TILE" />
      </intent-filter>
    </service>

    <service
      android:name="expo.modules.nidaavpn.MilitaryModeTileService"
      android:label="الخصوصية العسكرية"
      android:icon="@android:drawable/ic_lock_lock"
      android:exported="true"
      android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
      <intent-filter>
        <action android:name="android.service.quicksettings.action.QS_TILE" />
      </intent-filter>
    </service>

    <receiver
      android:name="expo.modules.nidaavpn.NidaaBootReceiver"
      android:enabled="true"
      android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED" />
        <action android:name="android.intent.action.QUICKBOOT_POWERON" />
      </intent-filter>
    </receiver>
  </application>
</manifest>
